# Handoff — 2026-08-14 (updated 2026-08-15)

*Last updated during session 2026-08-15.*

**Trigger:** `docs/superpowers/codex_recommendations.md` — a Codex review of the whole project (13 findings)

---

## TL;DR for the next session

One bug is **fixed, merged, and live on `master`**. Everything else is **design only** — no
other code changed. The Phase 1 safety spec is now at **v3**, with all 3 Criticals, 1 High, and
1 self-found defect fixed (see "What changed on 2026-08-15" below).

**The bot is disarmed.** `.env` now has `DRY_RUN=true`. Do not re-arm by flipping it back —
re-arming happens via `EXECUTION_MODE=live` once Phase 1 ships.

**Next step:** a fourth external review round on spec v3, then implement it.

---

## What changed on 2026-08-15

- **Merged and pushed** `chore/consolidate-codex-review` into `master` (`b07a31e`); branch deleted.
- **Disarmed `.env`** — `DRY_RUN=false` → `true`, with a comment explaining why and noting that
  `PAPER_TRADING` is inert.
- **Spec v3** on branch `docs/phase1-spec-v3` (`0555dca`), fixing:
  - **C1** kill switch never reached the sink → new `risk/kill_switch.py` holds process-wide
    runtime state; guard 2 *and* the sink both read it. (A `Config` field cannot work: `Config`
    reads env at construction and several call sites build their own instance.)
  - **C2** `submit_unknown` reserved no capital and could not be resolved → added to the
    committing and blocking status sets, never the terminal set; `/resolve` settles it via
    `get_orders_for_account` with an explicit 0 / 1 / 2+ / raises table. Submission outcomes are
    now classified, so a definitive 4xx is `submit_failed` and releases capital.
  - **C3** broker reads fail open → `raise_for_status` on every read, strict `parse_positions`,
    and a new `broker_unavailable` guard ordered *before* every guard that consumes broker data.
    **This one is a live defect in shipped code** and is sequenced (build step 3) to land alone.
  - **C4, self-found** the daily ceiling still bucketed on `'localtime'` — invalidated by
    `36761da` hours earlier. Would have reset `MAX_DAILY_NOTIONAL_USD` mid-session on this UTC+8
    host. Fixed here *and* at the source in `plans/2026-08-14-execution-ledger.md`.
  - **H1** `approved` blocked a ticker forever → the minimum order-status read is pulled into
    Phase 1 (one column, one broker call, one sweep). Terminal is an **allowlist**, because
    Schwab's enum has a literal `UNKNOWN` member.
  - Plus: sells are limit + DAY too, and a process rule (§14) requiring every "X handles this"
    claim to name a file that exists or is in the build sequence.
- All schwab-py facts re-verified against the installed **1.5.1**: `get_order`,
  `get_orders_for_account`, `cancel_order`, `equity_sell_limit`, and the status enum
  (`CANCELED`, one L, plus a literal `UNKNOWN`). **569 tests still pass.**

---

## Git state

**`master`** — PR #11 merged (`3189db9`). Contains everything below.

| Commit | What |
|---|---|
| `87a7339` | Phase 1 safety spec v1 |
| `a9a5328` | Backlog plans v1 (findings 4–13) |
| `d9e0f24` | Plans **v2** — fixed 4 Critical + 7 High from Codex review |
| `664a9e5` | Phase 1 spec **v2** — fixed 2 Critical + 5 High from Codex review |
| `36761da` | **fix: day-bucketing on US market session** ← the only code change |
| `128a64e` | This handoff |

**`chore/consolidate-codex-review`** — ✅ merged into `master` and pushed 2026-08-15
(`92fb25c`, `b07a31e`); branch deleted. Moved `codex_recommendations.md` from the repo root into
`docs/superpowers/` and repointed all six docs that cite it.

**`docs/phase1-spec-v3`** — 1 commit (`0555dca`), the v3 spec revision. Docs only.

---

## DONE — shipped code

**`36761da` — day-bucketing bug.** `ticker_recommended_today` bucketed on the *host* calendar
date. Host is Asia/Taipei (UTC+8); a US session crosses local midnight, so the configured
21:45 and 03:30 scans (= 09:45 ET and 15:30 ET, one session) landed on different local dates
and the dupe guard never fired. Same ticker could be bought twice per session.

Added `market_time.py` (`market_session_date`, `market_session_bounds_utc`; stdlib zoneinfo).
Fixed at all four sites: dupe guard, analyst quota (was resetting mid-session, doubling
`ANALYST_DAILY_LIMIT` to 36), `positions.entry_date`, and the `models.py` DEFAULT.
**569 tests pass** (546 + 23 new), ruff clean. CLAUDE.md convention corrected.

---

## NOT DONE — design docs awaiting implementation

All under `docs/superpowers/`:

| Doc | State |
|---|---|
| `specs/2026-08-14-live-trading-safety-design.md` | **v3 — all known defects fixed**, awaiting review round 4 |
| `plans/2026-08-14-codex-backlog-roadmap.md` | v2 — decomposition of findings 4–13 |
| `plans/2026-08-14-execution-ledger.md` | v2 + **v3 corrections** (status sets, session-bucketed `get_day_notional`). Findings 4, 10 |
| `plans/2026-08-14-analysis-integrity.md` | v2 — findings 5, 12. Bucketing math verified by execution |
| `plans/2026-08-14-screener-determinism.md` | v2 — findings 8, 13 |

---

## ⚠️ START HERE — review round 4, then implement

The three Criticals from review round 3, the High, and one self-found defect are **all fixed in
spec v3** (`0555dca`). See "What changed on 2026-08-15" above for what each fix is.

**Do this next:**

1. **Run review round 4.** The prompt is written and committed:

   ```bash
   codex exec --skip-git-repo-check < docs/superpowers/reviews/2026-08-15-spec-v3-review-prompt.md
   ```

   (A long prompt passed as an *argument* silently returns empty output — pipe the file.)
   It already carries the two recurring defect shapes, the focus areas, the environment facts,
   and Q1–Q3 framed as decisions to argue rather than confirm.
2. **Then implement**, following the v3 build sequence. Step 3 (`raise_for_status`) is a live
   defect in shipped code and can land on its own ahead of everything else.

**Five defects were already found in the v3 draft** by applying v3's own process rule to it —
`REPLACED` wrongly marked terminal, `/resolve` able to adopt a manually placed order, an
unconfirmed zero-match releasing capital, a silently stuck ticker, and unrecorded partial fills.
All are fixed or disclosed; the table is in the spec's "Corrections made to the v3 draft"
section. Mentioned here because it is evidence the rule works *and* evidence that self-review
alone still is not enough — three of the five were the same shape as the Criticals the rule was
written for.

**Standing bias to check for in review 4:** the recurring defect shape on this project is a
*prose claim naming no implementation*. v3 adds a rule against it (§14), but v3 was written by
the same author that rule is meant to police.

---

## Decisions already made (do not relitigate)

| Decision | Choice |
|---|---|
| Objective | Safe-to-trade first (Codex Phase 1), not backtesting first |
| Price drift at approval | Hard block + leave pending; no two-step confirm |
| Order type | **Limit + DAY** at `quote × 1.005`; `USE_LIMIT_BUY` removed |
| Sink guard | Require **both** `execution_mode == "live"` AND `not dry_run` — disagreement fails closed, keeps all 23 `dry_run=True` test guards working |
| `FUNDAMENTAL_MISSING_POLICY` | `reject` |
| Stop-loss (finding 9) | **Won't fix — by design.** Long-term hold strategy |
| Approach | Design → external review → revise → implement |

---

## Open questions for the user

1. **`SCAN_TIMEZONE` is unset.** Setting `America/New_York` would let `SCAN_TIMES` read as
   market times (`09:45,15:30`) instead of Taipei times needing mental conversion. One-line
   `.env` edit, not made.
2. **Sell-pass coherence.** Given long-term hold, the RSI>70 + MACD-bearish exit is a
   short-horizon reversal trigger that doesn't match the thesis. No downside exit, sensitive
   upside exit — backwards for buy-and-hold income. Recorded in the roadmap, unresolved.
3. **Schwab `FUNDAMENTAL` projection** — `Client.get_instruments(symbols, projection=FUNDAMENTAL)`
   returns `epsChangePercentTTM`, `returnOnEquity`, `currentRatio`, `marketCap`, etc. Could
   restore candidates lost to `reject` policy *and* supply the scale-independent factors
   findings 7/8 want. **Unverified** — the Schwab token is ~125 days old and expired. Re-auth
   with `python -c "from config import Config; from schwab_client.auth import get_client; get_client(Config())"`
   then one read-only call confirms the field names.

---

## Environment notes

- **schwab-py drift:** `requirements.txt:172` and `requirements.in:12` pin `1.4.0`; **1.5.1 is
  installed**. Every API fact in the plans (`get_order`, `get_quote`, status enum) was verified
  against 1.5.1. Bump the pin and regenerate the lock with `uv pip compile`, or re-verify.
- **Codex CLI:** upgraded 0.124.0 → 0.147.0 this session; re-authed via `codex login --device-auth`.
  Working. Review pattern used: write prompt to a file, `codex exec --skip-git-repo-check < file`
  (passing a long prompt as an argument silently produces empty output).
- **Live config:** `EXECUTION_MODE` does not exist yet. `.env` had `DRY_RUN=false` /
  `PAPER_TRADING=false` — **the bot was armed** with none of the Phase 1 guards built. **Disarmed
  2026-08-15 to `DRY_RUN=true`.** DB has 0 recommendations / 0 trades / 0 positions — nothing has
  ever executed, so the exposure was prospective throughout. Re-arm via `EXECUTION_MODE=live`
  after Phase 1 ships, never by flipping `DRY_RUN` back.
- **`PAPER_TRADING` is inert.** It is referenced only in the startup warning; Schwab has no
  paper endpoint. README/CLAUDE.md still claim it protects you until the spec lands.

---

## Environment changes made this session (not in git)

- **Skills are now global.** `npx skills add addyosmani/agent-skills --global --all` put 24
  skills in `~/.claude/skills` (29 total with agent-reach, playwright-cli, skill-creator,
  vault-ingest, vault-lint). The project-local copies in `.agents/` and `skills-lock.json`
  were removed from this repo; the 60 GSD skills in `.claude/skills` were left untouched.
  Removal needed `os.rmdir` — the links are Windows **junctions**, which `os.path.islink`
  reports as `False` and `shutil.rmtree` refuses.
- **Agent Reach installed** (`agent-reach` v1.5.0 via `uv tool install`) with `yt-dlp` and
  `mcporter`/Exa. 4/15 channels live without credentials. The other 8 need browser cookies;
  the project warns to use a secondary account. None configured.
- **Codex CLI** upgraded 0.124.0 → 0.147.0 and re-authed via `codex login --device-auth`.

## Backlog after Phase 1 (from the roadmap)

Dependency order — **only `C → D` is a hard chain**:

- **A — Execution ledger** (findings 4, 10): positions from confirmed fills. Gates *forward
  validation*, **not** backtesting (an earlier claim that A blocks C was wrong and is corrected
  in the roadmap).
- **B — Analysis integrity** (5, 12): cache key + TTL, untrusted-headline delimiting, ETF gate.
  Independent.
- **D0 — Screener determinism** (8, 13): missing-data policy, completed-bar volume. Independent.
- **C — Research harness** (6): blocked on a **point-in-time fundamentals data source**, not on A.
- **D — Signal redesign** (7, 11): blocked on C.

**Standing caveat:** no backtest exists and no forward sample exists. The 569 passing tests
validate software behavior, not profitability. Every recommendation is an unvalidated research
lead. Phase 1 makes the bot *safe to operate*; it does not make it *worth operating*.
