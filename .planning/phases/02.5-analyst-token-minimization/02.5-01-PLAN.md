# Phase 2.5: Analyst Token Minimization — COMPLETE

**Status:** ✅ Complete (2026-04-02). Plans 02.5-01 through 02.5-04 all executed.
**Requirements:** TOK-01 to TOK-06
**Branch merged:** feat/analyst-token-minimization (100 tests green)

## What was done

| Req | Change | File(s) |
|-----|--------|---------|
| TOK-01 | `TOP_SP500_COUNT=10` config field; `get_top_sp500_by_fundamentals(config)` ranks S&P by EPS+ROE, 24h in-memory cache | `config.py`, `screener/universe.py` |
| TOK-02 | `run_scan()` uses `get_top_sp500_by_fundamentals` (universe ~20 tickers) instead of full S&P 500 | `main.py` |
| TOK-03 | `analyst_cache` SQLite table keyed by `(ticker, headline_hash)` | `database/models.py`, `database/queries.py` |
| TOK-04 | `run_scan()`: SHA-256 headline hash → cache check before / store after `analyze_ticker` | `main.py` |
| TOK-05 | `ANALYST_CALL_DELAY_S=12.0`; `analyze_ticker` sleeps N seconds before `_call_api` (≤5 RPM) | `config.py`, `analyst/claude_analyst.py` |
| TOK-06 | `_wait_for_retry` parses Gemini `retryDelay` from 429 body; `_should_retry` skips retry if delay >60s | `analyst/claude_analyst.py` |

## Post-completion addition (2026-04-03)
Fallback provider: when primary `_call_api` raises after retries, `analyze_ticker()` retries with
`ANALYST_FALLBACK_PROVIDER` / `ANALYST_FALLBACK_MODEL`. Configured with Gemma 4 31B (same Gemini key).
See `analyst/claude_analyst.py`: `create_fallback_client()`.
