---
phase: 02.5-analyst-token-minimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.py, screener/universe.py]
autonomous: true
requirements: [TOK-01, TOK-02]
user_setup: []

must_haves:
  truths:
    - "config.top_sp500_count defaults to 50 and reads TOP_SP500_COUNT from env"
    - "config.analyst_call_delay_s defaults to 4.0 and reads ANALYST_CALL_DELAY_S from env"
    - "get_top_sp500_by_fundamentals(config) returns at most config.top_sp500_count tickers"
    - "get_top_sp500_by_fundamentals result is cached in-memory for 24h"
  artifacts:
    - path: config.py
      provides: Two new Config fields
      contains: "top_sp500_count"
    - path: screener/universe.py
      provides: Narrowed universe function
      contains: "get_top_sp500_by_fundamentals"
  key_links:
    - from: screener/universe.py
      to: get_sp500_tickers
      via: calls existing function to get full list before ranking
      pattern: "get_sp500_tickers"
---

<objective>
Add two config fields and implement get_top_sp500_by_fundamentals() to replace the raw S&P 500 list with a top-N by EPS+ROE ranking.

Purpose: Reduce analyst call count from ~500 to ~60 per scan (88% reduction).
Output: Config fields wired to env vars; new universe function with in-memory 24h cache.
</objective>

<execution_context>
@C:/Users/Darson/Projects/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Darson/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config.py
@screener/universe.py
@docs/superpowers/specs/2026-04-01-analyst-token-minimization-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOP_SP500_COUNT and ANALYST_CALL_DELAY_S to Config</name>
  <files>config.py</files>
  <read_first>config.py</read_first>
  <action>
In the Config dataclass, after the `scan_times` field, add:

    top_sp500_count: int = int(os.getenv("TOP_SP500_COUNT", "50"))
    analyst_call_delay_s: float = float(os.getenv("ANALYST_CALL_DELAY_S", "4.0"))

No changes to validate(). Preserve all existing fields and order.
  </action>
  <verify>python -c "from config import Config; c = Config(); assert c.top_sp500_count == 50; assert c.analyst_call_delay_s == 4.0; print('OK')"</verify>
  <acceptance_criteria>
    - config.py contains 'top_sp500_count: int = int(os.getenv("TOP_SP500_COUNT", "50"))'
    - config.py contains 'analyst_call_delay_s: float = float(os.getenv("ANALYST_CALL_DELAY_S", "4.0"))'
  </acceptance_criteria>
  <done>Both fields present with correct defaults; python -c import check passes</done>
</task>

<task type="auto">
  <name>Task 2: Add get_top_sp500_by_fundamentals() to universe.py</name>
  <files>screener/universe.py</files>
  <read_first>screener/universe.py, config.py</read_first>
  <action>
Add at the bottom of screener/universe.py (after get_sp500_tickers):

1. Module-level in-memory cache variable:
   _top_sp500_cache: dict = {}   # keys: "tickers" (list[str]), "fetched_at" (datetime)

2. New function:
   def get_top_sp500_by_fundamentals(config) -> list[str]:
       """
       Return top config.top_sp500_count S&P 500 tickers ranked by EPS + ROE.
       Uses in-memory 24h cache — yfinance info calls only happen once per day.
       Falls back to raw get_sp500_tickers() on any error.
       """
       global _top_sp500_cache
       if _top_sp500_cache.get("fetched_at"):
           age = (datetime.datetime.now() - _top_sp500_cache["fetched_at"]).total_seconds()
           if age < _CACHE_TTL_HOURS * 3600:
               return _top_sp500_cache["tickers"]

       tickers = get_sp500_tickers()
       scores: list[tuple[float, str]] = []
       for t in tickers:
           try:
               info = yf.Ticker(t).info
               eps = info.get("trailingEps") or 0.0
               roe = info.get("returnOnEquity") or 0.0
               scores.append((eps + roe, t))
           except Exception:
               continue

       scores.sort(key=lambda x: x[0], reverse=True)
       top = [t for _, t in scores[: config.top_sp500_count]]

       _top_sp500_cache = {"tickers": top, "fetched_at": datetime.datetime.now()}
       return top

Add `import yfinance as yf` at the top of universe.py (it is not currently imported there).
  </action>
  <verify>python -c "from screener.universe import get_top_sp500_by_fundamentals; print('import OK')"</verify>
  <acceptance_criteria>
    - screener/universe.py contains "def get_top_sp500_by_fundamentals"
    - screener/universe.py contains "_top_sp500_cache"
    - screener/universe.py contains "import yfinance as yf"
    - Function signature accepts a config parameter
  </acceptance_criteria>
  <done>Function importable; module-level cache initialized; yfinance imported</done>
</task>

</tasks>

<verification>
- [ ] python -c "from config import Config; c = Config(); assert c.top_sp500_count == 50" passes
- [ ] python -c "from screener.universe import get_top_sp500_by_fundamentals" passes (import only)
- [ ] pytest tests/ -x -q passes (no regressions)
</verification>

<success_criteria>
- Config has both new fields with correct env var names and defaults
- get_top_sp500_by_fundamentals importable with in-memory cache
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-analyst-token-minimization/02.5-01-SUMMARY.md`
</output>
