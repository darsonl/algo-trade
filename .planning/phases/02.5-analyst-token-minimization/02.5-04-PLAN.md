---
phase: 02.5-analyst-token-minimization
plan: 04
type: execute
wave: 2
depends_on: ["02.5-01", "02.5-02"]
files_modified: [main.py]
autonomous: true
requirements: [TOK-06]
user_setup: []

must_haves:
  truths:
    - "run_scan uses get_top_sp500_by_fundamentals(config) instead of get_sp500_tickers()"
    - "run_scan computes SHA-256 headline hash after fetch_news_headlines"
    - "run_scan checks analyst cache before calling analyze_ticker"
    - "run_scan writes analysis result to cache on cache miss"
  artifacts:
    - path: main.py
      provides: Wired-up token minimization in run_scan
      contains: "get_top_sp500_by_fundamentals"
  key_links:
    - from: main.py
      to: screener/universe.py
      via: get_top_sp500_by_fundamentals replaces get_sp500_tickers
      pattern: "get_top_sp500_by_fundamentals"
    - from: main.py
      to: database/queries.py
      via: get_cached_analysis / set_cached_analysis called per ticker
      pattern: "get_cached_analysis"
---

<objective>
Wire up the narrowed universe and analyst cache into run_scan() in main.py.

Purpose: Connect all Wave 1 changes into the live scan pipeline.
Output: run_scan uses top-50 S&P 500; skips analyst API for cached headlines; writes cache on miss.
</objective>

<execution_context>
@C:/Users/Darson/Projects/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Darson/Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@main.py
@.planning/phases/02.5-analyst-token-minimization/02.5-01-SUMMARY.md
@.planning/phases/02.5-analyst-token-minimization/02.5-02-SUMMARY.md
@docs/superpowers/specs/2026-04-01-analyst-token-minimization-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update imports in main.py</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
1. Replace the universe import line:
   BEFORE: `from screener.universe import get_watchlist, get_sp500_tickers, get_universe`
   AFTER:  `from screener.universe import get_watchlist, get_top_sp500_by_fundamentals, get_universe`

2. Add `import hashlib` after the existing stdlib imports (after `from pathlib import Path`).

3. The queries import already exists: `from database import queries` — no change needed.
  </action>
  <verify>python -c "import ast; ast.parse(open('main.py').read()); print('parse OK')"</verify>
  <acceptance_criteria>
    - main.py imports "get_top_sp500_by_fundamentals" (NOT get_sp500_tickers)
    - main.py contains "import hashlib"
    - main.py does NOT contain "get_sp500_tickers" in any import line
  </acceptance_criteria>
  <done>Imports updated; file parses without syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Replace S&amp;P 500 call and add cache check in run_scan()</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
In run_scan():

1. Replace the sp500 fetch block:
   BEFORE:
       try:
           sp500 = get_sp500_tickers()
       except Exception as exc:
           logger.warning("Could not fetch S&P 500 list: %s — using watchlist only", exc)
           sp500 = []

   AFTER:
       try:
           sp500 = get_top_sp500_by_fundamentals(config)
       except Exception as exc:
           logger.warning("Could not fetch top S&P 500: %s — using watchlist only", exc)
           sp500 = []

2. Inside the ticker loop, after `headlines = fetch_news_headlines(ticker)`, add cache check:
   BEFORE:
       headlines = fetch_news_headlines(ticker)
       analysis = analyze_ticker(ticker, info, headlines, config, client=client)

   AFTER:
       headlines = fetch_news_headlines(ticker)
       headline_hash = hashlib.sha256(
           "\n".join(sorted(headlines)).encode()
       ).hexdigest()
       cached = queries.get_cached_analysis(config.db_path, ticker, headline_hash)
       if cached:
           logger.debug("Cache hit for %s (hash %s...)", ticker, headline_hash[:8])
           analysis = cached
       else:
           analysis = analyze_ticker(ticker, info, headlines, config, client=client)
           queries.set_cached_analysis(
               config.db_path, ticker, headline_hash,
               analysis["signal"], analysis["reasoning"]
           )
  </action>
  <verify>python -c "import ast; ast.parse(open('main.py').read()); print('parse OK')"</verify>
  <acceptance_criteria>
    - main.py run_scan contains "get_top_sp500_by_fundamentals(config)"
    - main.py run_scan contains "hashlib.sha256"
    - main.py run_scan contains "queries.get_cached_analysis"
    - main.py run_scan contains "queries.set_cached_analysis"
    - main.py does NOT call "get_sp500_tickers()" in run_scan
  </acceptance_criteria>
  <done>Universe narrowed; cache check+set present; file parses without errors</done>
</task>

</tasks>

<verification>
- [ ] python -c "import ast; ast.parse(open('main.py').read())" passes
- [ ] pytest tests/ -x -q passes (no regressions)
- [ ] grep output shows get_top_sp500_by_fundamentals, headline_hash, get_cached_analysis all present in main.py
</verification>

<success_criteria>
- Universe uses get_top_sp500_by_fundamentals
- Cache check/set wired correctly in scan loop
- All imports valid; all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-analyst-token-minimization/02.5-04-SUMMARY.md`
</output>
