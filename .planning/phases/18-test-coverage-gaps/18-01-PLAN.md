---
phase: 18-test-coverage-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_analyst_claude.py
autonomous: true
requirements: [TEST-09]
must_haves:
  truths:
    - "A primary API failure on analyze_ticker triggers the fallback provider (test asserts fallback was called)"
    - "A parse error converges on the same fallback chain as an API error across all three analyst functions"
    - "analyze_sell_ticker has full fallback coverage (was the priority gap — previously zero fallback tests)"
    - "The fallback matrix (API-fail→fb, parse→fb, →fb2 chain, propagate-when-no-fb) is symmetric across analyze_ticker / analyze_etf_ticker / analyze_sell_ticker"
  artifacts:
    - path: "tests/test_analyst_claude.py"
      provides: "Fallback matrix tests for all three analyst functions"
      contains: "def test_analyze_sell_ticker_uses_fallback_on_primary_failure"
  key_links:
    - from: "tests/test_analyst_claude.py"
      to: "analyst.claude_analyst.analyze_sell_ticker"
      via: "patch('analyst.claude_analyst._call_api', side_effect=...)"
      pattern: "analyze_sell_ticker"
---

<objective>
Fill the analyst fallback matrix (TEST-09, D-01, D-04) so all three analyst functions —
`analyze_ticker`, `analyze_etf_ticker`, `analyze_sell_ticker` — have symmetric coverage of:
API-failure → fallback, parse-error → fallback, fallback → fallback2 chain, and
propagate-when-no-fallback. The sell path currently has ZERO fallback tests and is the priority gap.

Purpose: Lock in the reversed-spec behavior (parse errors ALSO trigger fallback — converging with
API errors on the `primary → fallback → fallback2` chain) and eliminate the sell-path blind spot.
Output: New test functions appended to tests/test_analyst_claude.py. No production code changes.
</objective>

<execution_context>
@C:/Users/Darson/Projects/algo trade/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Darson/Projects/algo trade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-test-coverage-gaps/18-CONTEXT.md

<interfaces>
<!-- Signatures the executor needs — extracted from analyst/claude_analyst.py. -->
<!-- Fallback path: try primary _call_api → parse; on ValueError OR any Exception, if fallback_client -->
<!-- is None re-raise, else try fallback; same again for fallback2. result["provider_used"] is set to -->
<!-- the provider name that succeeded. _make_fallback_config sets primary=gemini, fallback=deepseek, fallback2=openai. -->

def analyze_ticker(ticker, info, headlines, config, client=None, fallback_client=None,
                   macro_context=None, fundamental_trend=None, earnings_date=None,
                   fallback2_client=None) -> dict  # {"signal","reasoning","confidence","provider_used"}

def analyze_etf_ticker(ticker, headlines, tech_data, expense_ratio, config, client=None,
                       fallback_client=None, macro_context=None, fallback2_client=None) -> dict

def analyze_sell_ticker(ticker, entry_price, current_price, pnl_pct, hold_days, rsi, headlines,
                        config, client=None, fallback_client=None, macd_line=None, signal_line=None,
                        macro_context=None, info=None, fallback2_client=None) -> dict
# NOTE: client and fallback_client are POSITIONAL (9th/10th); fallback2_client is keyword-only in practice.
# Pass them as keywords to avoid arg-order mistakes: client=..., fallback_client=..., fallback2_client=...

# Existing reusable helper in the file (line ~810):
def _make_fallback_config():  # primary=gemini, fallback=deepseek(sk-test), fallback2=openai(openai-key), delay=0
</interfaces>

<coverage_matrix>
<!-- WHAT ALREADY EXISTS — DO NOT DUPLICATE. Fill only the MISSING cells. -->
analyze_ticker:
  - API-fail → fallback           : MISSING  ← add
  - parse-error → fallback        : EXISTS (test_analyze_ticker_uses_fallback_on_primary_parse_error)
  - → fallback2 chain (parse)     : EXISTS (test_analyze_ticker_uses_fallback2_on_fallback_parse_error)
  - → fallback2 chain (API-fail)  : MISSING  ← add
  - propagate when no fallback    : EXISTS (test_analyze_ticker_parse_error_propagates_when_no_fallback;
                                            also test_analyze_ticker.py::test_analyze_ticker_propagates_parse_error)
analyze_etf_ticker:
  - API-fail → fallback           : EXISTS (test_analyze_etf_ticker_uses_fallback_on_primary_failure)
  - parse-error → fallback        : EXISTS (test_analyze_etf_ticker_uses_fallback_on_primary_parse_error)
  - → fallback2 chain (API-fail)  : EXISTS (test_analyze_etf_ticker_uses_fallback2_when_both_primary_and_fallback_fail)
  - → fallback2 chain (parse)     : MISSING  ← add
  - propagate when no fallback    : EXISTS (test_analyze_etf_ticker_propagates_when_fallback_fails_and_no_fallback2)
analyze_sell_ticker:
  - ALL FOUR CELLS MISSING — priority gap. Add the full set.
</coverage_matrix>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analyze_sell_ticker full fallback matrix (priority gap)</name>
  <files>tests/test_analyst_claude.py</files>
  <read_first>
    - tests/test_analyst_claude.py (the file being modified — note `_make_fallback_config` at ~line 810
      and the existing analyze_ticker/analyze_etf_ticker fallback tests at lines 826-905 to mirror)
    - analyst/claude_analyst.py lines 537-619 (analyze_sell_ticker — the function under test; note the
      try/except ValueError then except Exception structure repeated for primary/fallback)
    - tests/test_sell_scan.py lines 42-51 (TECH_DATA constants and the kind of sell inputs used elsewhere)
  </read_first>
  <behavior>
    - Primary API raises → fallback called, provider_used == "deepseek", exactly 2 _call_api calls
    - Primary returns template-echo (parse error) → fallback called, provider_used == "deepseek", 2 calls
    - Primary + fallback both API-raise → fallback2 called, provider_used == "openai", 3 calls
    - Primary + fallback both return template-echo (parse error) → fallback2 called, provider_used == "openai", 3 calls
    - Primary fails (API) with fallback_client=None → exception propagates (pytest.raises)
  </behavior>
  <action>
    Append a section header `# --- analyze_sell_ticker fallback matrix (D-04 priority gap) ---` and add
    these test functions to tests/test_analyst_claude.py. Import analyze_sell_ticker at top of file
    (extend the existing `from analyst.claude_analyst import ...` line). Use the existing
    `_make_fallback_config()` helper (primary=gemini, fallback=deepseek, fallback2=openai). Use the
    `call_count = {"n": 0}` + `side_effect` sequencing pattern exactly as the existing parse-error tests do.

    Call analyze_sell_ticker with KEYWORD args to avoid the ~13-positional-arg trap:
      analyze_sell_ticker(ticker="AAPL", entry_price=150.0, current_price=170.0, pnl_pct=0.13,
                          hold_days=10, rsi=75.0, headlines=["Headline"], config=config,
                          client=MagicMock(), fallback_client=MagicMock(), fallback2_client=MagicMock())
    (drop fallback2_client where the test does not need it; set fallback_client=None for the propagate test).

    Add:
    1. `test_analyze_sell_ticker_uses_fallback_on_primary_failure` — side_effect raises RuntimeError("quota
       exhausted") on call 1, returns "SIGNAL: SELL\nREASONING: Fallback confirmed overbought.\nCONFIDENCE: high"
       on call 2. assert result["signal"]=="SELL"; assert result["provider_used"]=="deepseek"; assert call_count["n"]==2.
    2. `test_analyze_sell_ticker_uses_fallback_on_primary_parse_error` — call 1 returns the template-echo
       string "SIGNAL: <SELL|HOLD>\nREASONING: template echo" (unparseable → ValueError), call 2 returns a
       valid "SIGNAL: HOLD\nREASONING: Fallback says hold.\nCONFIDENCE: medium". assert provider_used=="deepseek",
       call_count==2, signal=="HOLD".
    3. `test_analyze_sell_ticker_uses_fallback2_when_both_primary_and_fallback_fail` — calls 1-2 raise
       RuntimeError("provider down"), call 3 returns valid SELL. Pass fallback2_client=MagicMock().
       assert provider_used=="openai", call_count==3.
    4. `test_analyze_sell_ticker_uses_fallback2_on_fallback_parse_error` — calls 1-2 return template-echo
       (parse errors), call 3 returns valid "SIGNAL: SELL\nREASONING: fb2 confirmed.\nCONFIDENCE: low".
       Pass fallback2_client=MagicMock(). assert provider_used=="openai", call_count==3.
    5. `test_analyze_sell_ticker_propagates_when_no_fallback` — _call_api side_effect always raises
       RuntimeError("all down"); call with fallback_client=None. Wrap in `with pytest.raises(RuntimeError,
       match="all down")`.

    Patch via `with patch("analyst.claude_analyst._call_api", side_effect=api_side_effect):` (the same
    module-level patch target the existing tests use). Do NOT assert parse errors skip fallback — that is
    the reversed/wrong behavior.
  </action>
  <verify>
    <automated>cd "C:/Users/Darson/Projects/algo trade" && python -m pytest tests/test_analyst_claude.py -q -k analyze_sell_ticker</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_analyst_claude.py -q -k analyze_sell_ticker` exits 0
    - All five named functions exist: grep -c "def test_analyze_sell_ticker_" tests/test_analyst_claude.py >= 5
    - The string "SIGNAL: <SELL|HOLD>" (template-echo, the parse-error trigger) appears in the file
    - NO assertion in any added test claims a parse error skips fallback (grep for "skip" near fallback returns nothing asserting that)
  </acceptance_criteria>
  <done>analyze_sell_ticker has all four matrix cells plus propagate-no-fallback covered; sell-path blind spot closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill remaining analyze_ticker and analyze_etf_ticker matrix gaps</name>
  <files>tests/test_analyst_claude.py</files>
  <read_first>
    - tests/test_analyst_claude.py (coverage_matrix in this plan lists exactly which cells exist; add only
      the three MISSING cells listed below)
    - tests/test_analyze_ticker.py lines 43-47 (existing API-fail propagate test for analyze_ticker — do
      not duplicate; it already covers propagate-when-no-fallback for the API-failure case)
    - analyst/claude_analyst.py lines 310-384 (analyze_ticker) and 387-468 (analyze_etf_ticker)
  </read_first>
  <behavior>
    - analyze_ticker: primary API-raise → fallback called, provider_used=="deepseek", 2 calls
    - analyze_ticker: primary+fallback API-raise → fallback2 called, provider_used=="openai", 3 calls
    - analyze_etf_ticker: primary+fallback template-echo (parse) → fallback2 called, provider_used=="openai", 3 calls
  </behavior>
  <action>
    Append to tests/test_analyst_claude.py, reusing `_make_fallback_config()`, the `call_count`+`side_effect`
    pattern, and `with patch("analyst.claude_analyst._call_api", side_effect=...)`.

    Add exactly these three (the only missing cells — see coverage_matrix; do NOT re-add the parse-error
    analyze_ticker tests that already exist):
    1. `test_analyze_ticker_uses_fallback_on_primary_failure` — call 1 raises RuntimeError("quota exhausted"),
       call 2 returns "SIGNAL: BUY\nREASONING: Fallback confirmed.\nCONFIDENCE: high". Call:
       analyze_ticker(ticker="BLK", info={}, headlines=[], config=config, client=MagicMock(),
       fallback_client=MagicMock()). assert provider_used=="deepseek", call_count==2, signal=="BUY".
    2. `test_analyze_ticker_uses_fallback2_on_fallback_api_failure` — calls 1-2 raise RuntimeError("down"),
       call 3 returns valid "SIGNAL: SKIP\nREASONING: fb2.\nCONFIDENCE: low". Pass fallback2_client=MagicMock().
       assert provider_used=="openai", call_count==3.
    3. `test_analyze_etf_ticker_uses_fallback2_on_fallback_parse_error` — calls 1-2 return template-echo
       "SIGNAL: <BUY|HOLD|SKIP>\nREASONING: template echo" (parse errors), call 3 returns valid
       "SIGNAL: BUY\nREASONING: fb2 confirmed.\nCONFIDENCE: medium". tech_data = {"rsi":60.0,"macd_line":0.1,
       "signal_line":0.05,"macd_histogram":0.05,"price":450.0,"ma50":440.0}; call analyze_etf_ticker(
       ticker="SPY", headlines=[], tech_data=tech_data, expense_ratio=0.0009, config=config,
       client=MagicMock(), fallback_client=MagicMock(), fallback2_client=MagicMock()). assert
       provider_used=="openai", call_count==3.
  </action>
  <verify>
    <automated>cd "C:/Users/Darson/Projects/algo trade" && python -m pytest tests/test_analyst_claude.py -q -k "fallback"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_analyst_claude.py -q -k "fallback"` exits 0
    - These three new functions exist (grep): test_analyze_ticker_uses_fallback_on_primary_failure,
      test_analyze_ticker_uses_fallback2_on_fallback_api_failure,
      test_analyze_etf_ticker_uses_fallback2_on_fallback_parse_error
    - No duplicate of an existing test name (grep -c each name == 1)
  </acceptance_criteria>
  <done>analyze_ticker and analyze_etf_ticker matrices are complete and symmetric with the sell path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundaries. This plan adds test code only; no runtime code paths, endpoints, or inputs change.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | (informational) | tests/test_analyst_claude.py | accept | Test-only phase, no new attack surface. Mocks replace all network/SDK calls; no secrets, no I/O. No threats above informational. |
</threat_model>

<verification>
- Full analyst test file passes: `python -m pytest tests/test_analyst_claude.py -q` exits 0
- No regressions in the broader analyst suite: `python -m pytest tests/test_analyze_ticker.py -q` exits 0
- Total fallback-matrix coverage: every (function × failure-type) cell in the coverage_matrix is now present
</verification>

<success_criteria>
- analyze_sell_ticker fallback coverage exists where there was none (priority gap closed)
- All three analyst functions cover API-fail→fb, parse→fb, →fb2 chain, and propagate-when-no-fb
- Parse-error behavior asserts CONVERGENCE on the fallback chain (never skip) — SC#1 and SC#2 satisfied
- No existing test duplicated
</success_criteria>

<output>
After completion, create `.planning/phases/18-test-coverage-gaps/18-01-SUMMARY.md`
</output>
