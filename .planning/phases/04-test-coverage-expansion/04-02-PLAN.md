# Phase 4: Test Coverage Expansion — COMPLETE (Plan 04-02)

**Status:** ✅ Complete (2026-04-03)
**Requirements covered:** TEST-02, TEST-03, TEST-07, TEST-08

| Req | Test file | Tests | What's covered |
|-----|-----------|-------|----------------|
| TEST-02 | `tests/test_discord_buttons.py` | 7 | dry-run no order, dry-run trade with null order_id, live place_order call, live order_id stored, status "approved", confirmation message, zero-shares guard |
| TEST-03 | `tests/test_discord_buttons.py` | 3 | status "rejected", confirmation message sent, place_order not called |
| TEST-07 | `tests/test_database.py` | 8 | fresh pending True, different ticker False, empty DB False, expired False, rejected False, approved True, UTC yesterday False, UTC today True |
| TEST-08 | `tests/test_database.py` | 5 | fresh stays pending, approved not overwritten, rejected not overwritten, 1-second-past expires, exact-now stays pending |

**Deviation:** discord.py wraps `@discord.ui.button` methods into Button objects; tests invoke via
`view.approve.callback.callback(view, interaction, button)` — correct workaround, coverage unaffected.
